const { app, nativeImage } = require('electron'); const fs = require('fs'); const path = require('path'); app.whenReady().then(() => { const img = nativeImage.createFromPath(path.join(__dirname, 'public', 'raven.webp')); const buf = img.toPNG(); fs.writeFileSync(path.join(__dirname, 'public', 'raven.png'), buf); fs.writeFileSync(path.join(__dirname, 'build', 'icon.png'), buf); console.log('exiting...'); app.quit(); });
