const { app, nativeImage } = require('electron'); const fs = require('fs'); const path = require('path'); app.whenReady().then(() = const image = nativeImage.createFromPath(path.join(__dirname, 'public', 'raven.webp')); const buffer = image.toPNG(); fs.writeFileSync(path.join(__dirname, 'public', 'raven.png'), buffer); fs.writeFileSync(path.join(__dirname, 'build', 'icon.png'), buffer); console.log('Done'); app.quit(); });  
