# Shared Contracts

Contratos compartidos para la migracion a microservicios.

- `events.py`: sobre `EventEnvelope`.
- `jobs.py`: contratos de mensajes `candidates`, `validated`, `jobs`.

Objetivo en Fase 0: centralizar tipos base para evitar definiciones duplicadas entre servicios.

